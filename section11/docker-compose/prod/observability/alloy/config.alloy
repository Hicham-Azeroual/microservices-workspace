logging {
  level = "info"
}

loki.source.api "containers" {
  http {
    listen_address = "0.0.0.0"
    listen_port    = 12345
  }
  forward_to = [loki.write.gateway.receiver]
}

loki.write "gateway" {
  endpoint {
    url = "http://gateway:3100/loki/api/v1/push"
  }
}